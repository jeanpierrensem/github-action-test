name: git-hub-action-test
on: push

jobs:
  hello-world-job:
    runs-on: ubuntu-latest
    
    steps: 
    - name : use node.js
      uses: actions/setup-node@v4
      with : 
          node-version : '24.x'
    - name: checkout-repository-code
      uses : actions/checkout@v4
    - run : echo "${cat hello_world.txt}" 

      
